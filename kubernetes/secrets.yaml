apiVersion: v1
kind: Secret
metadata:
  name: elasticsearch-secret
  namespace: loganalyzer
  labels:
    app: loganalyzer
type: Opaque
data:
  # Base64 encoded values - replace with your actual credentials
  username: ZWxhc3RpYw==  # 'elastic' base64 encoded
  password: Y2hhbmdlbWU=   # 'changeme' base64 encoded
---
# To create the secret with actual values, run:
# kubectl create secret generic elasticsearch-secret \
#   --namespace loganalyzer \
#   --from-literal=username=elastic \
#   --from-literal=password=your-actual-password