apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loganalyzer-wal
  namespace: loganalyzer
  labels:
    app: loganalyzer
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard  # Change this to match your cluster's storage class
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loganalyzer-buffers
  namespace: loganalyzer
  labels:
    app: loganalyzer
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loganalyzer-dlq
  namespace: loganalyzer
  labels:
    app: loganalyzer
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loganalyzer-archive
  namespace: loganalyzer
  labels:
    app: loganalyzer
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard